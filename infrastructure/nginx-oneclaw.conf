# OneClaw API nginx config
# Place in /etc/nginx/sites-available/oneclaw

server {
    listen 80;
    server_name oneclaw.chat api.oneclaw.chat;

    # Redirect HTTP to HTTPS (uncomment after SSL is set up)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:4001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Important for Discord interactions
        proxy_read_timeout 60s;
        proxy_connect_timeout 60s;
    }
}

# HTTPS config (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name oneclaw.chat api.oneclaw.chat;
#
#     ssl_certificate /etc/letsencrypt/live/oneclaw.chat/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/oneclaw.chat/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:4001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
