{
  "name": "@oneclaw/workflows",
  "version": "0.1.0",
  "description": "Workflow definitions for OneClaw - audit, discovery, analysis, etc.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsx watch src/server.ts",
    "audit": "tsx src/audit-website/cli.ts",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@oneclaw/clients": "workspace:*",
    "@oneclaw/engine": "workspace:*",
    "@oneclaw/taxonomy": "workspace:*",
    "cheerio": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
